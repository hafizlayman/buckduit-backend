services:
  buckduit-ai-core:
    envs:
      - SUPABASE_URL
      - SUPABASE_SERVICE_KEY
      - TELEGRAM_BOT_TOKEN
      - TELEGRAM_CHAT_ID
      - OPENAI_API_KEY
      - FLASK_ENV
      - PORT
    startCommand: python buckduit_ai_core.py
    autoDeploy: true
    restartPolicyType: always
